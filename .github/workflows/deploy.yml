name: Build & Deploy to Arvan

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Login to Arvan Registry
        run: echo "${{ secrets.ARVAN_REGISTRY_PASSWORD }}" | docker login registry.arvan.ir -u "${{ secrets.ARVAN_REGISTRY_USER }}" --password-stdin

      - name: Build image
        run: |
          docker build \
            --build-arg NEXT_PUBLIC_SUPABASE_URL="${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}" \
            --build-arg NEXT_PUBLIC_BASE_URL="${{ secrets.NEXT_PUBLIC_BASE_URL }}" \
            --build-arg NEXT_PUBLIC_APP_NAME="${{ secrets.NEXT_PUBLIC_APP_NAME }}" \
            -t registry.arvan.ir/${{ secrets.ARVAN_NAMESPACE }}/zendegi:latest \
            -t registry.arvan.ir/${{ secrets.ARVAN_NAMESPACE }}/zendegi:${{ github.sha }} \
            .

      - name: Push image
        run: |
          docker push registry.arvan.ir/${{ secrets.ARVAN_NAMESPACE }}/zendegi:latest
          docker push registry.arvan.ir/${{ secrets.ARVAN_NAMESPACE }}/zendegi:${{ github.sha }}
